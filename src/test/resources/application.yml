spring:
    datasource:
        url: jdbc:h2:mem:test #메모리 모드로 동작
        username: sa
        password:
        driver-class-name: org.h2.Driver


    jpa:
        hibernate:
            ddl-auto: create
        properties:
           hibernate:
#          show_sql: true
              format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
#    org.hibernate.type: trace
# 테스트와 실제개발 application.yml 따로 나누는게 좋다.

